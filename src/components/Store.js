import React from 'react';

const Store = (props)=>{
  return(
    <div>
      <h1>this is the store for now</h1>
      <h2>you are currently in the {props.location}</h2>
    </div>
  )
}

export default Store;